basePath: /
definitions:
  services.MockUser:
    properties:
      email:
        type: string
      id:
        type: integer
      name:
        type: string
    type: object
host: localhost:8080
info:
  contact: {}
  description: This is a SimpleGoApi server.
  title: SimpleGoApi API
  version: "1.0"
paths:
  /users:
    get:
      description: Get all users
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/services.MockUser'
      summary: Get users
swagger: "2.0"
